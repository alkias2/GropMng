@model GropMng.Web.Areas.Admin.Models.Owner.OwnerEditModel
@{
    ViewData["Title"] = "Edit Owner";
    GropHtml.SetActiveMenuItemSystemName("Owners");

    var listUrl = Url.Action("List", "Owner", new { area = "Admin" })!;
    var headerModel = new AdminPageHeaderModel
    {
        Title = string.IsNullOrWhiteSpace(Model.DisplayName) ? "Edit Owner" : Model.DisplayName,
        Breadcrumbs = new List<AdminBreadcrumbItemModel>
        {
            new() { Text = "Dashboard", Url = Url.Action("Index", "Home", new { area = "" }) },
            new() { Text = "Owners", Url = listUrl },
            new() { Text = "Edit Owner" }
        },
        ActionButtons = new Microsoft.AspNetCore.Html.HtmlString(
            "<a href=\"" + listUrl + "\" class=\"btn btn-label-secondary\"><i class=\"bx bx-arrow-back me-1\"></i> " + T("common.cancel") + "</a>" +
            " <button type=\"submit\" form=\"ownerEditForm\" class=\"btn btn-primary\"><i class=\"bx bx-save me-1\"></i> " + T("common.save") + "</button>")
    };
}

@await Html.PartialAsync("_AdminPageHeader", headerModel)
@await Html.PartialAsync("Notifications")

<div class="card">
    <div class="card-body">
        <form id="ownerEditForm" asp-area="Admin" asp-controller="Owner" asp-action="Edit" method="post">
            @Html.AntiForgeryToken()
            <input asp-for="OwnerId" type="hidden" />
            @await Html.PartialAsync("_CreateOrUpdate", Model)
        </form>
    </div>
</div>
